name: flame_debug
description: "Flame debug project."
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=3.2.0-210.4.beta <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  flame: ^1.10.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
